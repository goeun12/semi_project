	<select id="checkId" resultType="_int">
		select count(*) from member where id = #{id}
	</select>
	
	<insert id="insertMember">
		insert into member
		values(#{name}, #{id}, #{pwd}, #{phone}, default, default, default)
	</insert>
